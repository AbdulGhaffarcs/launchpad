# Operating Launchpad

To feature a project, open its file in `src/content/projects/` and change `featured: false` to `featured: true`. Keep only one featured project.

To remove a bad submission, delete its project file and push. Vercel will redeploy automatically.

Students never need to edit this repository.
